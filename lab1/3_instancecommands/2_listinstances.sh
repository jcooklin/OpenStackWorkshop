#Command to list all the instances available on the cloud
#We can check the VM we just built using the below command
#This wil  display the workshopinstance created  name, status and IP. 
#Once the server is UP the status should show as Active

nova list
